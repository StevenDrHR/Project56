<!DOCTYPE html>
<html lang="en">
<head>
    <style> table, tr, tbody {border: 1px solid black; }</style>
    <style> td button {width: 50 px ; height: 50 px;}  </style>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <meta charset="UTF-8">

    <title>Shop Homepage - Start Bootstrap Template</title>

    <!-- Bootstrap Core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="css/shop-homepage.css" rel="stylesheet">

</head>
<body>
<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="http://localhost:4567/Home">Homepage</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <a href="http://localhost:4567/Register">About</a>
                </li>
                <li>
                    <a href="http://localhost:4567/Register">Register</a>
                </li>
                <li>
                    <a href="http://localhost:4567/Register">Contact</a>
                </li>
                <li id="adminvisibility">
                    <a href="http://localhost:4567/Adminpage">Adminpage</a>
                </li>
                <form3>
                    <input type="text" placeholder="search" align="right">
                    <button type="submit" id="search_button" align="right">Search</button>
                </form3>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container -->
</nav>
<form class="form2" method='POST' action='/Adminpage'>

<table style="width:100%" id="admintable" >
    <tbody>

    </tbody>
</table>
</form>
   <script>
        if("${userlevel}" == "admin"){
              var a = document.querySelector("#adminvisibility");
              a.style.display = "initial";
          }
   </script>

   <script>
        var dirtystring = "${users}";
        var cleanstring = dirtystring.substr(1, dirtystring.length - 2)
        var parts = cleanstring.split(", ");


       for (var i = 0; i < parts.length; i++){

           table = document.getElementById("admintable");
           Deletebutton =  document.createElement("button");
           Modifybutton =  document.createElement("button");

           //making the buttons
           Deletebutton.setAttribute("name", "Delete "+ parts[i]);
           Deletebutton.setAttribute("type", "submit");
           Deletebutton.innerHTML = "Delete";
           Modifybutton.setAttribute("name", "Modify "+ parts[i]);
           Modifybutton.setAttribute("type", "submit");
           Modifybutton.innerHTML = "Modfiy";
           //adding the row
           row = table.insertRow(i);
           row.setAttribute("name", ""+parts[i]);

           //making the amount of values in a row
           cell1 = row.insertCell(0);
           cell2 = row.insertCell(1);
           cell3 = row.insertCell(2);

           //adding values
           cell1.innerHTML = parts[i];
           cell2.appendChild(Deletebutton);
           cell3.appendChild(Modifybutton);
       }
   </script>
</body>
</html>